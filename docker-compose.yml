services:
  pipeline:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    restart: unless-stopped
